% Function for DNA encoding based on rule 06
%------------------------------------------------------------------------
  function d  =  DNA_encode_rule06(P,mn)
%------------------------------------------------------------------------
% STEP 1:Creating an empty string  array for DNA  encoding
%------------------------------------------------------------------------
  d   =  [];
  k1  =  [];
  k2  =  [];
 
%------------------------------------------------------------------------
% STEP 2:DNA Convertion table -rule 06
%------------------------------------------------------------------------

% Checking first two bits 
%------------------------------------------------------------------------
  for i = 1:mn   
   
  %___      
      if x(1) == '0' && x(2) == '0'  
        k1 = 'G';
    elseif x(1) == '0' && x(2) == '1'
        k1 = 'T';  
    elseif x(1) == '1' && x(2) == '0'
        k1 = 'A';
    elseif x(1) == '1' && x(2) == '1'    
        k1 = 'C';
      end
      
%-------------------------------------------------------------------------
 % Checking next two bits
%-------------------------------------------------------------------------
  
  
  
  
  
  
  % if need complete code contact me
  
  
  
  
  
  
  
  
  
%-------------------------------------------------------------------------
 % Checking next two bits
%-------------------------------------------------------------------------
   

% STEP 3:DNA Encoding Output
   
%-------------------------------------------------------------------------      
 end
